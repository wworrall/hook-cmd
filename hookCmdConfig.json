{
  "hookCmds": [
    {
      "hook": "a secret key to identify this hook (e.g., use uuidgen). Hook will be listened for at HOST:PORT/hook",
      "cmd": "the command string to execute (see: https://nodejs.org/api/child_process.html#child_process_child_process_exec_command_options_callback)",
      "childProcessOptions": "options object passed to child_process.exec()"
    }
  ],
  "port": 5000
}
